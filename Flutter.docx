Flutter Tutorial for Beginners #2 - Flutter Overview
------------------------------------------------------------------
everything is widget

#Widget

1. Text widget
2. Button Widget
3. Row Widget
4. Colmn Widget
5. Image Widget

------------------------------------------------------------------
Flutter Tutorial for Beginners #3 - Dart Primer
----------------------------------------------------------------------

void main(){
string name = 'dalton';
name = '30';
print (name);
}

................................................................
"DYnamic data class" ----- can be used to change the values of anytime (not realted to single data types)
................................................................
"Arrow function"
 
for example:

 void main()
{
int age = getage();
print(age);
}

int getage() => 30;    

.......................................................
"List" Same as array just new name;
but we can print it easily like

void main()
{
List names=['dalton','darwin'];
print(names);
}

to add new members --------- 

names.add('deelok');

to remove members --------

names.remove('dalton');

we can keep any type of data inside the list but if we want a single datatype to be assigned inside the list
to do that we need to add <data type> after List keyword

for example

List<string> name=['dalton','darwin'];

...............................................................
 Dart is OOP
so we do have classes
class: blueprint of objects(have methods and data)
same as C++ for declaration

class User{
string username='mario';
int age=25;

void login()
{
print('user logged in');
}


void main()
{
//creating an object
User dalton = User();
//dalton is the object name
print(dalton.age);
dalton.login();

..............................................................

for inheritance

keyword "extends"

class superuser extends User{}

........................................................................
#5
..................................................................

makiing an flutter proejct from stratch

import 'package:flutter/material.dart';
---> this imports the dart language

void main() => runApp(MaterialApp(
home: Text(Hey Wagwan!),
));

MaterialApp()---->it is a root widget that Use google material its like wrapper for rest of our widgets
----> we should put comma after our proterty value 

#5----> just made android stuido operating(checking)

-------------------------------------------------------------------
#6
---------------------------------------------------------------------
